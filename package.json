{
  "name": "multiplayer",
  "version": "1.0.0",
  "description": "A simple multiplayer game",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --watch js --watch server.js server.js",
    "first-deploy": "git commit --allow-empy -m 'Deploying' && git push heroku deploy:master",
    "deploy": "git pull heroku master && git commit --allow-empy -m 'Deploying' && git push heroku deploy:master"
  },
  "author": "Sarah Charles",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.0.1",
    "express": "^4.15.3",
    "phaser": "^2.6.2",
    "socket.io": "^2.0.3",
    "volleyball": "^1.4.1"
  }
}
